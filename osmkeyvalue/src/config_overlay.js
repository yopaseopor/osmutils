// Overlay configuration for OSM Utils
// This provides the overlay structure for the original overlay searcher

export const overlayConfig = {
    overlays: []
};

// This file can be extended to include overlay definitions
// For now, it provides an empty structure that the overlay system expects
